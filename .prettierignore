dist
target
node_modules
src/lib/ipc-types.ts
src-tauri/gen
package-lock.json
Cargo.lock

# Prose is authored, not generated. Legal documents in particular should not be
# reflowed by a formatter — the wording and emphasis are deliberate.
*.md
docs/

# Hand-formatted style models; the grouping carries meaning.
data/

# Generated by scripts/vendor-fonts.mjs — 546 @font-face rules straight from the
# Google Fonts API. Reformatting them would produce a large, meaningless diff on
# every re-vendor and tell us nothing; the shape is asserted by fonts.test.ts.
src/assets/fonts/fonts.css
src/assets/fonts/fonts-scripts.css
